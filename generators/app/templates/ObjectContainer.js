import { connect } from 'react-redux';
import <%= name %>View from './<%= name %>View';
import {

} from './<%= name %>State';

export default connect(
  state => ({
}),
dispatch => ({
})
)(<%= name %>View);
